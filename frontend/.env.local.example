# The Laravel API. Server-side only -- the browser never talks to it directly.
API_URL=http://localhost:8000/api

# Used to sign nothing yet, but required by Next for production builds.
NEXT_PUBLIC_APP_NAME="WB PMS"
